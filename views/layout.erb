<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Compliance Viewer aims to make the ATO process and regulatory compliance more tangible, actionable, and transparent for project teams.">
    <link href="<%= stylesheet_path 'stylesheets/app' %>" rel="stylesheet" />
    <title>Compliance Viewer</title>
    <link rel="shortcut icon" type="image/x-icon" href="/images/favicon.ico" />
  </head>

  <body>

    <%= erb 'partials/header'.to_sym %>

    <section class="content">
      <div class="usa-grid">
        <%= yield %>
        </div>
      </div>
    </section>

    <%= erb 'partials/footer'.to_sym %>

    <script src="<%= javascript_path 'javascripts/app' %>"></script>
  </body>
</html>
